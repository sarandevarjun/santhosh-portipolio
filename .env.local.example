# Set to true when Strapi is running (npm run develop in tvk-strapi)
NEXT_PUBLIC_USE_STRAPI=true
STRAPI_URL=http://localhost:1337

# Optional: API token from Strapi Admin → Settings → API Tokens (Read-only)
# STRAPI_API_TOKEN=
